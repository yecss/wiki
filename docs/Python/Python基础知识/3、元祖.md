## 概念

什么是元祖：不可变的列表称为元祖。

定义方法：使用()来定义

```python
dimension = (200,50)
print(dimension)
print(dimension[0])
print(dimension[1])
```

## 遍历元祖

```python
dimensions = (200, 50)
for dimension in dimensions:
    print(dimension)
```

